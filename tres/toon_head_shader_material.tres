[gd_resource type="ShaderMaterial" format=3 uid="uid://drvngci832ple"]

[ext_resource type="Texture2D" uid="uid://pdy4fj8jbyho" path="res://data/3d/textures/head_tex.png" id="1_vf0lv"]

[sub_resource type="Shader" id="Shader_gevx8"]
code = "shader_type spatial;
render_mode unshaded, depth_draw_always;

uniform sampler2D albedo_texture : source_color;

uniform vec3 light_direction = vec3(0.0, -1.0, -1.0);
uniform float shade_threshold = 0.3;
uniform float dark_shade = 0.8;

void fragment() {
    vec3 n = normalize(NORMAL);
    vec3 l = normalize(-light_direction);

    float intensity = dot(n, l);
    float shade = intensity > shade_threshold ? 1.0 : dark_shade;

    vec4 tex_color = texture(albedo_texture, UV);
    ALBEDO = tex_color.rgb * shade;
    ALPHA = tex_color.a;
}"

[resource]
render_priority = 0
shader = SubResource("Shader_gevx8")
shader_parameter/albedo_texture = ExtResource("1_vf0lv")
shader_parameter/light_direction = Vector3(0, -1, -1)
shader_parameter/shade_threshold = 0.3
shader_parameter/dark_shade = 0.8
